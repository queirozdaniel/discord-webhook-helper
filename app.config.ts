export default defineAppConfig({
    project: {
      name: "Discord Webhook Helper",
      description: "Projeto destinado para salvar modelos de \"personagens\" usados para enviar mensagem no Discord via Webhook."
    },
    nuxtIcon: {
      size: '24px', // default <Icon> size applied
      class: 'icon', // default <Icon> class applied
      aliases: {
        'nuxt': 'logos:nuxt-icon',
      }
    },
})
  